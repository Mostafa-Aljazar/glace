# Glace API base URL (must include /api)
# The backend is the single source of truth for home, events and menu data —
# there is no local fake-data mode.
NEXT_PUBLIC_API_URL=https://glace-bzjj.onrender.com/api
