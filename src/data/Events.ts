/**
 * @deprecated Prefer `@/data/fake-data/events` and `@/types/events.types`.
 * Kept for generateStaticParams / legacy imports.
 */
export {
  FAKE_EVENTS as EVENTS,
  EVENTS_PER_PAGE as ITEMS_PER_PAGE,
} from "@/data/fake-data/events";
export type { IEvent as EventData } from "@/types/events.types";
